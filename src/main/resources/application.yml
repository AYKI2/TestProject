spring:
  application:
    name: "TestProject"
  datasource:
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL95Dialect
        format_sql: true

server:
  port: 5656

logging:
  level:
    root: INFO

#jwt
jwt:
  secret: "bWJhbmstdGVzdC1wcm9qZWN0LXNlY3JldC1rZXk="
  lifetime: "5m"